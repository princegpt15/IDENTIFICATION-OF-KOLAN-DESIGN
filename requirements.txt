# Python Requirements for Kolam Pattern Classification System
# Steps 1 & 2: Dataset Preparation and Preprocessing

# Core dependencies
opencv-python>=4.8.0        # Image processing (preprocessing pipeline)
numpy>=1.24.0               # Array operations
pandas>=2.0.0               # Data handling and annotations
matplotlib>=3.7.0           # Visualization and validation plots

# Progress visualization
tqdm>=4.65.0                # Progress bars for batch processing

# Optional but recommended
Pillow>=10.0.0              # Additional image processing
scikit-learn>=1.3.0         # For advanced splitting utilities (future use)

# Development dependencies (optional)
ipython>=8.12.0             # Interactive Python shell
jupyter>=1.0.0              # For notebooks (if needed)

# Note: For Step 3 (CNN training), additional requirements will include:
# tensorflow>=2.13.0 or pytorch>=2.0.0
# These will be added in Step 3
